package {{package}};

import org.springframework.cloud.openfeign.FeignClient;
import com.ayagmar.jobscraper.model.JobListingResponse;
import com.ayagmar.jobscraper.model.HTTPValidationError;
import com.ayagmar.jobapplicationtracker.configuration.FeignClientConfig;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import java.util.List;

@FeignClient(name = "{{classname}}Client", url = "${job.api.base-url}", configuration=FeignClientConfig.class)
public interface {{classname}} {

@GetMapping("/api/v1/jobs")
List
<JobListingResponse> getJobs();

    @GetMapping("/api/v1/scrape-jobs")
    List
    <JobListingResponse> scrapeJobs(
        @RequestParam("job_title") String jobTitle,
        @RequestParam(value = "country", defaultValue = "us") String country,
        @RequestParam(value = "pages", defaultValue = "1") Integer pages,
        @RequestParam(value = "source", defaultValue = "indeed") String source
        );
        }
